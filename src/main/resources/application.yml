spring:
  application:
    name: Zomato-Clone-API

  #DataSource Configuration
  datasource:
    url: jdbc:mysql://localhost:3306/zomato_db/?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl_auto: update
    properties:
      hibernate:
        format_sql: true

